# MrMarket 前端镜像（开发模式）
FROM node:22-alpine

WORKDIR /app

# 安装依赖
COPY ui/package.json ui/package-lock.json ./
RUN npm install

# 复制源码
COPY ui/ .

# Vite 开发服务器端口
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
