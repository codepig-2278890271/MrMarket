mrmarket/
│
├── frontend/                # 前端（React）：用户界面（图表、输入框、AI对话、页面展示）
│   ├── src/                 # 前端源码（页面、组件、状态管理、路由）
│   ├── public/              # 静态资源（图片、favicon、html模板）
│   └── package.json         # 前端依赖管理（React/Echarts/Axios等）
│
├── backend/                 # 后端（FastAPI）：数据处理、AI分析、股票逻辑
│   ├── app/
│   │   ├── api/             # 接口层（提供给前端的HTTP API，如/stock /ai /news）
│   │   ├── services/        # 业务层（股票分析、AI分析、回测、策略逻辑）
│   │   ├── integrations/    # 外部数据接入（AkShare、东方财富、DeepSeek等）
│   │   ├── models/          # 数据结构（Stock、User、Strategy等实体定义）
│   │   ├── utils/           # 工具函数（时间处理、计算、格式化等）
│   │   └── main.py          # 后端入口（启动FastAPI应用）
│   │
│   ├── tests/               # 测试代码（接口测试、逻辑测试）
│   ├── pyproject.toml       # Python依赖管理（类似package.json）
│   └── .env                 # 环境变量（API Key、数据库密码等）
│
├── ai/                      # AI层（Prompt / Agent / Tool / Config）
│   ├── prompts/             # 提示词（股票分析、新闻总结、策略建议）
│   ├── agents/              # AI代理（分析、研究、风控、多步骤推理）
│   ├── tools/               # AI工具（行情获取、新闻获取、计算工具）
│   ├── config/              # 模型配置（DeepSeek/Claude参数）
│   └── core/                # AI统一入口（封装LLM调用逻辑）
│
├── scripts/                 # 脚本工具（开发/数据/回测/启动）
│   ├── dev/                 # 开发脚本（启动前后端、环境初始化）
│   ├── data/                # 数据脚本（AkShare同步、行情更新）
│   └── backtest/            # 回测脚本（策略批量运行）
│
├── docs/                    # 项目文档（设计+说明）
│   ├── architecture.md      # 系统架构设计
│   ├── product.md           # 产品需求（MVP/功能规划）
│   ├── api.md               # 接口文档
│   ├── ai.md                # AI设计说明（prompt/agent逻辑）
│   └── decisions.md         # 关键设计决策记录（为什么这样设计）
│
├── docker/                  # 容器化配置（开发/部署环境）
│
├── .github/                 # CI/CD（自动测试、自动部署流程）
│
├── .gitignore               # Git忽略规则（不提交敏感信息）
├── .refactor.md             # 重构记录（代码结构优化、模块拆分）
└── README.md                # 项目说明（如何启动 + 项目简介）